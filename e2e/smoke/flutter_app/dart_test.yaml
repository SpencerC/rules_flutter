tags:
  golden:
    # Golden tests are regenerated by the flutter_goldens target
    # (flutter test --update-goldens --run-skipped); skip them in normal
    # flutter_test runs so they never compare against a checked-in baseline.
    skip: true
