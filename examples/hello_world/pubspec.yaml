name: hello_world
description: A minimal Flutter application built with rules_flutter.

version: 1.0.0+1

environment:
  flutter: ">=3.24.0"
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
