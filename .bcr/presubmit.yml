bcr_test_module:
  module_path: "e2e/smoke"
  matrix:
    # The action layer is POSIX-only today; Windows returns with the
    # cross-platform action-runner workstream. Bazel 9+ only.
    platform: ["macos", "ubuntu2204"]
    bazel: ["9.0.0", "9.x"]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
