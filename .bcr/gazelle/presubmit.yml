matrix:
  platform: ["macos", "ubuntu2204"]
  bazel: ["9.0.0", "9.x"]
tasks:
  verify_targets:
    name: "Build and test the Gazelle plugin"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    build_targets:
      - "@rules_flutter_gazelle//..."
    test_targets:
      - "@rules_flutter_gazelle//..."
